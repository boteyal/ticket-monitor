[phases.setup]
nixPkgs = ["...", "glib", "nss", "nspr", "atk", "cups", "dbus", "libdrm", "gtk3", "pango", "cairo", "alsa-lib", "mesa", "xorg.libX11", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXfixes", "xorg.libXrandr", "xorg.libxcb"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install-deps chromium"
]

[start]
cmd = "python ticket_monitor.py"
